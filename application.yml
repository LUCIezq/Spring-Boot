spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gestion_db
    username: appuser
    password: apppass

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true